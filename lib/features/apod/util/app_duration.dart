abstract class AppDuration {
  static const shortDuration = Duration(milliseconds: 200);
  static const mediumDuration = Duration(milliseconds: 600);
  static const longDuration = Duration(seconds: 1, milliseconds: 200);
}
